$schema: 'https://moonrepo.dev/schemas/workspace.json'

projects:
  - 'packages/*'
  - 'templates/*'

vcs:
  manager: 'git'
  defaultBranch: 'main'
  provider: 'github'

generator:
  templates:
    - './templates'
